package com.practice.zooticketportal.repositories;

import com.practice.zooticketportal.entity.TicketEntryFees;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TicketEntryFeesRepo extends JpaRepository<TicketEntryFees,Long> {
}
